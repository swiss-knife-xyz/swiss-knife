// Metadata is put inside the page to handle searchParams

const CalldataDecoderLayout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default CalldataDecoderLayout;
